{
  "name": "recommendation-validator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/app.ts --bundle --platform=node --minify --outfile=build/app.js",
    "quick-deploy-recommendo": "npm run build && cd build && zip -r build.zip . && aws lambda update-function-code --function-name 'recommendation-validator' --zip-file fileb://build.zip --profile recommendo --region ca-central-1",
    "test": "vitest run --coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^20.12.7",
    "@vitest/coverage-v8": "0.34.1`",
    "esbuild": "^0.14.14",
    "typescript": "^5.4.5",
    "vitest": "0.34.1"
  },
  "dependencies": {
    "mongodb": "^6.5.0"
  }
}
