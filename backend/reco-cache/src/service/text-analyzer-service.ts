export const findKeywords = (text: string, amount: number): string[] => {
    return [].slice(0, amount);
}

export const optimizeText = (text: string): Promise<string> => {
    return Promise.resolve(text);
}